<%= simple_form_for(@subject) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :subID %>
    <%= f.input :Name %>
    <%= f.input :sem %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
